import { PagePlaceholder } from "@/components/common/page-placeholder";

export default function StudentsPage() {
  return (
    <PagePlaceholder
      title="Students"
      description="Manage enrollment records, student profiles, and status tracking."
    />
  );
}
